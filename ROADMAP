Luca.Collection.query
  - add function pipeline to run filter, and options 
    objects through.  anything can inject itself
    into the pipeline.  

    basically, remove this from the view.

  - write a backbone-query -> squeel -> activerecord bridge


