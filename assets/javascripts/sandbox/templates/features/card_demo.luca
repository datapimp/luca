%h3 Card Container
%p The Card Container is a wizard style container which handles the display / hiding of a single frame at a time. 
%p The individual demos below are a collection of nested containers displayed within a Card View.
